// mehndi-design-response.model.ts (if using a separate file)
export interface AddMehndiDesignResponse {
    message: string;
    designId?: number;
    // Add any other properties that your API response might include
  }
  